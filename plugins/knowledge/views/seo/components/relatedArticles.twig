<section>
    <h2>{{ title }}</h2>
    <ul>
        {% for article in articles %}
            <li>
                <a href="{{ article.url }}">
                    <h3>{{ article.name }}</h3>
                    {% if article.excerpt %}
                        <p>{{ article.excerpt }}</p>
                    {% endif %}
                </a>
            </li>
        {% endfor %}
    </ul>
    <a href="{{ moreUrl }}">
        {{ moreLabel }}
    </a>
</section>
